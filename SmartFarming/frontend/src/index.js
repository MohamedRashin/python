import React from "react";
import ReactDOM from "react-dom/client";
import CropRecommendationApp from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <React.StrictMode>
        <CropRecommendationApp />
    </React.StrictMode>
);
